- probleme index parcour de trad pour GEN
- flask noninstall replit
- change print into return forgot in if
- cannot name files flask.py : error partial installation
- button css
- center stackoverflow
- diff between redirect and render
- main.css wrong call in results.html (resulting in 404 in console)
- difficulties pour trouver plusieurs acides aminé dans le même ARNm (diff de format)
- problème avec les requêtes POST (rajout d'un nouveau argument)
- unittest cannot do test : "ImportError: attempted relative import with no known parent package", needs to be transformed into a python package
- canvas ne veut pas fonctionner, solution : attendre que le DOM charge